<?xml version="1.0" encoding="utf-8"?>
<resources>
    <attr name="text" format="string" />
    <attr name="color" format="color" />
    <attr name="size" format="dimension" />

    <declare-styleable name="CustomText">
        <attr name="text" />
        <attr name="color" />
        <attr name="size" />
    </declare-styleable>

    <declare-styleable name="FanChart">
        <attr name="isShowInnerText" format="boolean" />
        <attr name="innerCircleRadius" format="float" />
        <attr name="innerTextColor" format="color" />
        <attr name="innerCircleColor" format="color" />
        <attr name="radius" format="dimension" />
        <attr name="innerText" format="string" />
        <attr name="minAngle" format="integer" />
        <attr name="textColor" format="color" />
        <attr name="textSize" format="dimension" />
        <attr name="innerTextSize" format="dimension" />
    </declare-styleable>


    <declare-styleable name="TestView">
        <attr name="texts" format="string" />
    </declare-styleable>

    <!--

        private var bgColor = Color.YELLOW // 默认背景透明
    private var bgPaintColor = Color.parseColor("#f2f2f2") // 默认进度条背景色
    private var proPaintColor = Color.parseColor("#2c99ff") // 默认进度条颜色
    private var paintWidth = 10f // 默认画笔宽度
    private var startAngle = 180f // 开始位置
    private var sweepAngle = 180f // 圆弧弧度
    -->
    <declare-styleable name="RingProgressView">
        <attr name="paintWidth" format="dimension|reference" />
        <attr name="startAngle" format="dimension|reference" />
        <attr name="sweepAngle" format="dimension|reference" />
        <attr name="bgColor" format="color|reference" />
        <attr name="bgPaintColor" format="color|reference" />
        <attr name="proPaintColor" format="color|reference" />
    </declare-styleable>
</resources>